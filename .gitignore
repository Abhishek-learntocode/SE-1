### ===========================
### .gitignore for Apache Roller Weblogger + Refactoring Lab
### ===========================

# ─────────────────────────────────────────────
# IDE / Editor configs
# ─────────────────────────────────────────────

# Eclipse – project metadata generated by Eclipse IDE (classpath, project
# descriptor, and per-project settings folder).  Every developer's Eclipse
# config is different; committing these causes constant merge conflicts.
.classpath
.project
.settings/

# IntelliJ IDEA – the .idea/ directory stores project-level IDE settings
# (run configs, inspections, code style, etc.) that are machine-specific.
# .iml / .iws files are module and workspace descriptors.
.idea/
*.iml
*.iws

# NetBeans – nbproject/ and nb-configuration.xml hold local build configs
# and project metadata that are specific to the NetBeans IDE.
nb-configuration.xml
nbproject/
nbactions.xml

# VS Code – .vscode/ contains workspace settings (settings.json, tasks,
# launch configs). These are local preferences and shouldn't be shared
# across contributors unless the team explicitly agrees to check them in.
.vscode/

# SonarLint – .sonarlint/ contains Connected Mode config that ties the
# local IDE to a specific SonarQube/SonarCloud server.  This is
# developer-specific and not portable across environments.
.sonarlint/

# ─────────────────────────────────────────────
# OS-generated files
# ─────────────────────────────────────────────

# macOS – .DS_Store is a Finder metadata file created automatically in
# every folder; it has no relevance to the project.
.DS_Store

# Windows – Thumbs.db and Desktop.ini are Explorer metadata files.
Thumbs.db
Desktop.ini

# ─────────────────────────────────────────────
# Java / Maven build output
# ─────────────────────────────────────────────

# target/ – Maven writes all compiled classes, packaged WARs/JARs,
# test reports (surefire), and intermediate build artifacts here.
# This is fully reproducible via `mvn package` so never needs tracking.
target/

# log/ – some Maven plugins or CI systems write logs into a top-level
# log/ directory during builds.
log/

# ─────────────────────────────────────────────
# Apache Roller runtime artifacts
# ─────────────────────────────────────────────

# velocity.log – Apache Velocity template engine writes runtime logs
# to this file at the working directory level.
velocity.log

# tomcat-base – local embedded Tomcat directory created during
# development testing; contains transient server state.
tomcat-base/

# app/logs/ – application runtime logs generated by Log4j during
# local testing.  These grow large and are not source code.
app/logs/

# ─────────────────────────────────────────────
# Docker data volumes
# ─────────────────────────────────────────────

# postgresql-data / roller-data – Docker Compose mounts these as
# persistent volumes for the PostgreSQL database and Roller media
# uploads.  They contain binary DB pages and user uploads—large,
# machine-specific, and not part of the source tree.
docker/postgresql-data/
docker/roller-data/

# ─────────────────────────────────────────────
# Release / CI build artifacts
# ─────────────────────────────────────────────

# release.sh is generated by the Maven release plugin with GPG keys
# and local paths baked in.  Never commit credentials or local paths.
assembly-release/release.sh

# Selenium integration-test overlay directory generated by the Maven
# WAR plugin; these are extracted dependency files, not source.
it-selenium/overlays/

# ─────────────────────────────────────────────
# Python artifacts  (Refactoring Lab scripts)
# ─────────────────────────────────────────────

# __pycache__/ and *.pyc – CPython bytecode cache.  Generated
# automatically when any .py file is imported/executed.  These are
# architecture-specific and fully reproducible.
__pycache__/
*.pyc
*.pyo

# ─────────────────────────────────────────────
# Refactoring Lab – large binary tools
# ─────────────────────────────────────────────

# The bin/ folder contains third-party analysis tool JARs (Checkstyle
# ~12 MB, CK Metrics ~18 MB, DesigniteJava ~8 MB, PMD ~40 MB).
# Binary distribution archives should not live in Git—they bloat the
# repo history and can be re-downloaded. Track download instructions
# in a README instead.
Refactoring_Lab/bin/*.jar
Refactoring_Lab/bin/pmd-bin/

# ─────────────────────────────────────────────
# Refactoring Lab – generated analysis outputs
# ─────────────────────────────────────────────

# Plot images (PNGs) in the data archive are regenerated by the
# simple_plots.py / generate_comparison.py scripts from the CSV data.
# Keeping 96+ PNGs in Git adds bulk; they can be rebuilt on demand.
Refactoring_Lab/data_archive/**/simple_plots/
Refactoring_Lab/data_archive/**/comparison/

# NOTE: The CSV files (class.csv, method.csv) and XML reports
# (checkstyle_report.xml, pmd_report.xml) in data_archive/ ARE
# intentionally tracked—they are the primary metrics record for
# each refactoring round and cannot be easily regenerated after
# the source has changed.

# ─────────────────────────────────────────────
# Temporary / scratch files at repo root
# ─────────────────────────────────────────────

# intial.txt – a 9 000-line directory-tree dump used for one-off
# reference.  Not part of the project source.
intial.txt

# remains.java – leftover scratch file, not part of the build.
remains.java

# sonar-project.properties – local SonarQube scanner config pointing
# to a specific server URL and project key.  Usually contains
# developer-specific values; keep it out of the shared repo.
# (Remove this line if your team standardises a shared config.)
sonar-project.properties

# ─────────────────────────────────────────────
# Miscellaneous binary / generated patterns
# ─────────────────────────────────────────────

# Compiled Java class files outside of target/ (safety net).
*.class

# Package files – JARs, WARs, EARs should come from the build, not
# from version control.
*.jar
*.war
*.ear

# Log files that may appear anywhere.
*.log

# Backup / swap files from editors.
*.swp
*.swo
*~
*.bak